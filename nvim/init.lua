-- === Neovim Configuration ===

-- Load options
require("config.options")

-- Load diagnostic config
require("config.diagnostic")

-- Load keymaps
require("config.keymaps")

-- Load lazy.nvim (which loads all plugins)
require("config.lazy")
